import { FormComponentsComponent } from './form-components.component';

describe('FormComponentsComponent', () => {
  let component: FormComponentsComponent;

  beforeEach(() => {
    component = new FormComponentsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
