import { WebSdkComponent } from './web-sdk.component';

describe('WebSdkComponent', () => {
  let component: WebSdkComponent;

  beforeEach(() => {
    component = new WebSdkComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
