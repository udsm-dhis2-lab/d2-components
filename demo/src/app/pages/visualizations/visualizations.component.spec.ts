import { VisualizationsComponent } from './visualizations.component';

describe('VisualizationsComponent', () => {
  let component: VisualizationsComponent;

  beforeEach(() => {
    component = new VisualizationsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
