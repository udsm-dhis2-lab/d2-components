import { OrganisationUnitSelectorComponent } from './organisation-unit-selector-modal.component-modal';

describe('OrganisationUnitSelectorComponent', () => {
  let component: OrganisationUnitSelectorComponent;

  beforeEach(() => {
    component = new OrganisationUnitSelectorComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
