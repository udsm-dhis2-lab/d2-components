import { PeriodSelectorComponent } from './period-selector.component';

describe('PeriodSelectorComponent', () => {
  let component: PeriodSelectorComponent;

  beforeEach(() => {
    component = new PeriodSelectorComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
