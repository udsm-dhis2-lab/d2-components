import { PeriodSelectorModule } from './period-selector.module';

describe('PeriodSelectorModule', () => {
  const module: PeriodSelectorModule = new PeriodSelectorModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
