import { TrackerFormComponent } from './tracker-form.component';

describe('TrackerFormComponent', () => {
  let component: TrackerFormComponent;

  beforeEach(() => {
    component = new TrackerFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
