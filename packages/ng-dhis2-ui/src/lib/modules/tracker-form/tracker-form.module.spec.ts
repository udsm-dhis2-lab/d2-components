import { TrackerFormModule } from './tracker-form.module';

describe('TrackerFormModule', () => {
  const module: TrackerFormModule = new TrackerFormModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
