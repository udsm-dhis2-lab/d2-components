import { TransferFieldComponent } from "./transfer-field.component";

describe('TextFieldComponent', () => {
  let component: TransferFieldComponent;

  beforeEach(() => {
    component = new TransferFieldComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
