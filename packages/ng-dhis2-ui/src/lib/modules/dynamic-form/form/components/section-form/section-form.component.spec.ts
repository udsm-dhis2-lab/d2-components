import { SectionFormComponent } from './section-form.component';

describe('SectionFormComponent', () => {
  let component: SectionFormComponent;

  beforeEach(() => {
    component = new SectionFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
