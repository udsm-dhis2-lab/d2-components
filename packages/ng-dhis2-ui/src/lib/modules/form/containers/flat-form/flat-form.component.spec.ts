import { FlatFormComponent } from './flat-form.component';

describe('FlatFormComponent', () => {
  let component: FlatFormComponent;

  beforeEach(() => {
    component = new FlatFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
