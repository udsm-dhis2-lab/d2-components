import { BreadcrumbModule } from './breadcrumb.module';

describe('BreadcrumbModule', () => {
  const module: BreadcrumbModule = new BreadcrumbModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
