import { ButtonModule } from './button.module';

describe('ButtonModule', () => {
  const module: ButtonModule = new ButtonModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
