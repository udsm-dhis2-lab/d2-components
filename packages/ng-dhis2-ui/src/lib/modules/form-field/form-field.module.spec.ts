import { FormFieldModule } from './form-field.module';

describe('FormFieldModule', () => {
  const module: FormFieldModule = new FormFieldModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
