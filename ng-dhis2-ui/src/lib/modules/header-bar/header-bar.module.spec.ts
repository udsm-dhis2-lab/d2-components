import { HeaderBarModule } from './header-bar.module';

describe('HeaderBarModule', () => {
  const module: HeaderBarModule = new HeaderBarModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
